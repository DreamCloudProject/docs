---
title: 'BillingServiceImpl'
description: 'Aufruf öffentlicher Servicemethoden über die REST API: billingServiceImpl'
---

# BillingServiceImpl

<ResponseField name="beanId" type="string">

Die Spring-Bean-ID des aufzurufenden Services.

billingServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Bean-Scope. Für Serviceaufrufe wird PROTOTYPE verwendet.

</ResponseField>

<ResponseField name="functionName" type="string">

Der öffentliche Methodenname des Services, der in der Signatur angegeben ist.

</ResponseField>

<ResponseField name="args" type="object[]">

Das Argument-Array der Methode. Jedes Argument wird mit dem Index 0, 1 usw. übergeben; der Wert muss dem Java-Parametertyp entsprechen.

</ResponseField>

## Endpunkt und Autorisierung

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Zugriff:** ADMIN. **Bearer Token:** erforderlich.

- GUEST — nicht angemeldete Benutzer; nur Whitelist-Services sind verfügbar.
- USER — Authentifizierung erforderlich; Methoden mit USER-Prüfung sind verfügbar.
- ADMIN — vollständiger Zugriff auf öffentliche Methoden aller Services.
- Wenn eine Methode intern eine Rolle prüft, kann GUEST null erhalten.

## Methoden

<ResponseField name="getServiceFee" type="BigDecimal">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| — | — | Diese Methode akzeptiert keine Argumente. |

### getServiceFee

```json
{
  "beanId": "billingServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getServiceFee",
  "args": []
}
```

</ResponseField>

<ResponseField name="calculateServiceFee" type="BigDecimal">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `amount` | `BigDecimal` | Übergeben über `args[0].0`. |

### calculateServiceFee

```json
{
  "beanId": "billingServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "calculateServiceFee",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="calculateServiceFeeNettoAmount" type="BigDecimal">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `order` | `Order` | Übergeben über `args[0].0`. |

### calculateServiceFeeNettoAmount

```json
{
  "beanId": "billingServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "calculateServiceFeeNettoAmount",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="calculateServiceFeeBruttoAmount" type="BigDecimal">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `order` | `Order` | Übergeben über `args[0].0`. |

### calculateServiceFeeBruttoAmount

```json
{
  "beanId": "billingServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "calculateServiceFeeBruttoAmount",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="calculatePayoutAmount" type="BigDecimal">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `order` | `Order` | Übergeben über `args[0].0`. |

### calculatePayoutAmount

```json
{
  "beanId": "billingServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "calculatePayoutAmount",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="calculateItemsTotalAmount" type="BigDecimal">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `items` | `Collection<CommerceItem>` | Übergeben über `args[0].0`. |

### calculateItemsTotalAmount

```json
{
  "beanId": "billingServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "calculateItemsTotalAmount",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="calculateItemsTotalBruttoAmount" type="BigDecimal">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `amount` | `BigDecimal` | Übergeben über `args[0].0`. |
| `taxAmount` | `BigDecimal` | Übergeben über `args[1].1`. |

### calculateItemsTotalBruttoAmount

```json
{
  "beanId": "billingServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "calculateItemsTotalBruttoAmount",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="calculateTaxAmount" type="BigDecimal">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `amount` | `BigDecimal` | Übergeben über `args[0].0`. |
| `taxRate` | `BigDecimal` | Übergeben über `args[1].1`. |

### calculateTaxAmount

```json
{
  "beanId": "billingServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "calculateTaxAmount",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="calculateDeliveryAmount" type="BigDecimal">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `order` | `Order` | Übergeben über `args[0].0`. |

### calculateDeliveryAmount

```json
{
  "beanId": "billingServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "calculateDeliveryAmount",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="calculateBruttoAmount" type="BigDecimal">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `items` | `List<CommerceItem>` | Übergeben über `args[0].0`. |
| `tax` | `Order.Tax` | Übergeben über `args[1].1`. |

### calculateBruttoAmount

```json
{
  "beanId": "billingServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "calculateBruttoAmount",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="calculateNettoAmount" type="BigDecimal">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `items` | `List<CommerceItem>` | Übergeben über `args[0].0`. |

### calculateNettoAmount

```json
{
  "beanId": "billingServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "calculateNettoAmount",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
