---
title: 'commerceItemServiceImpl'
description: 'Aufruf öffentlicher Servicemethoden über die REST API: commerceItemServiceImpl'
---

# commerceItemServiceImpl

<ResponseField name="beanId" type="string">

Die Spring-Bean-ID des aufzurufenden Services.

commerceItemServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Bean-Scope. Für Serviceaufrufe wird PROTOTYPE verwendet.

</ResponseField>

<ResponseField name="functionName" type="string">

Der öffentliche Methodenname des Services, der in der Signatur angegeben ist.

</ResponseField>

<ResponseField name="args" type="object[]">

Das Argument-Array der Methode. Jedes Argument wird mit dem Index 0, 1 usw. übergeben; der Wert muss dem Java-Parametertyp entsprechen.

</ResponseField>

## Endpunkt und Autorisierung

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Zugriff:** ADMIN. **Bearer Token:** erforderlich.

- GUEST — nicht angemeldete Benutzer; nur Whitelist-Services sind verfügbar.
- USER — Authentifizierung erforderlich; Methoden mit USER-Prüfung sind verfügbar.
- ADMIN — vollständiger Zugriff auf öffentliche Methoden aller Services.
- Wenn eine Methode intern eine Rolle prüft, kann GUEST null erhalten.

## Methoden

<ResponseField name="getRepository" type="CommerceItemRepository">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| — | — | Diese Methode akzeptiert keine Argumente. |

### getRepository

```json
{
  "beanId": "commerceItemServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getRepository",
  "args": []
}
```

</ResponseField>

<ResponseField name="calculateTotalPrice" type="BigDecimal">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `items` | `Collection<CommerceItem>` | Übergeben über `args[0].0`. |

### calculateTotalPrice

```json
{
  "beanId": "commerceItemServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "calculateTotalPrice",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="mapItems" type="List<CommerceItemDTO>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `items` | `Collection<CommerceItem>` | Übergeben über `args[0].0`. |

### mapItems

```json
{
  "beanId": "commerceItemServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "mapItems",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="extractSpecs" type="Map<String, Object>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `sku` | `SellerSKU` | Übergeben über `args[0].0`. |

### extractSpecs

```json
{
  "beanId": "commerceItemServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "extractSpecs",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="removeCommerceItem" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `id` | `String` | Übergeben über `args[0].0`. |

### removeCommerceItem

```json
{
  "beanId": "commerceItemServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "removeCommerceItem",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="removeCommerceItems" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `ids` | `List<String>` | Übergeben über `args[0].0`. |

### removeCommerceItems

```json
{
  "beanId": "commerceItemServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "removeCommerceItems",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="refundCommerceItems" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `order` | `Order` | Übergeben über `args[0].0`. |

### refundCommerceItems

```json
{
  "beanId": "commerceItemServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "refundCommerceItems",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="updateInventoriesLockedQuantity" type="boolean">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `map` | `Map<CommerceItem, Inventory>` | Übergeben über `args[0].0`. |
| `increase` | `boolean` | Übergeben über `args[1].1`. |

### updateInventoriesLockedQuantity

```json
{
  "beanId": "commerceItemServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "updateInventoriesLockedQuantity",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="obtainItemToInventoryMap" type="Map<CommerceItem, Inventory>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `items` | `List<CommerceItem>` | Übergeben über `args[0].0`. |

### obtainItemToInventoryMap

```json
{
  "beanId": "commerceItemServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "obtainItemToInventoryMap",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="extractAttributeValues" type="List<Map<String, Object>>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `sku` | `SellerSKU` | Übergeben über `args[0].0`. |

### extractAttributeValues

```json
{
  "beanId": "commerceItemServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "extractAttributeValues",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
