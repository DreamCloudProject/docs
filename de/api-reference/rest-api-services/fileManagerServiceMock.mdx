---
title: 'FileManagerServiceMock'
description: 'Aufruf öffentlicher Servicemethoden über die REST API: fileManagerServiceMock'
---

# FileManagerServiceMock

<ResponseField name="beanId" type="string">

Die Spring-Bean-ID des aufzurufenden Services.

fileManagerServiceMock

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Bean-Scope. Für Serviceaufrufe wird PROTOTYPE verwendet.

</ResponseField>

<ResponseField name="functionName" type="string">

Der öffentliche Methodenname des Services, der in der Signatur angegeben ist.

</ResponseField>

<ResponseField name="args" type="object[]">

Das Argument-Array der Methode. Jedes Argument wird mit dem Index 0, 1 usw. übergeben; der Wert muss dem Java-Parametertyp entsprechen.

</ResponseField>

## Endpunkt und Autorisierung

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Zugriff:** ADMIN. **Bearer Token:** erforderlich.

- GUEST — nicht angemeldete Benutzer; nur Whitelist-Services sind verfügbar.
- USER — Authentifizierung erforderlich; Methoden mit USER-Prüfung sind verfügbar.
- ADMIN — vollständiger Zugriff auf öffentliche Methoden aller Services.
- Wenn eine Methode intern eine Rolle prüft, kann GUEST null erhalten.

## Methoden

<ResponseField name="createDirectory" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `path` | `Path` | Übergeben über `args[0].0`. |

### createDirectory

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "createDirectory",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="findByPath" type="List<File>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `path` | `Path` | Übergeben über `args[0].0`. |

### findByPath

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "findByPath",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="findByPath" type="List<File>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `path` | `Path` | Übergeben über `args[0].0`. |
| `filter` | `FileFilter` | Übergeben über `args[1].1`. |

### findByPath

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "findByPath",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="findFiles" type="List<File>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `path` | `Path` | Übergeben über `args[0].0`. |

### findFiles

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "findFiles",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="findFiles" type="List<File>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `path` | `Path` | Übergeben über `args[0].0`. |
| `fileExtension` | `String` | Übergeben über `args[1].1`. |

### findFiles

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "findFiles",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="findRootFolders" type="List<File>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `rootPath` | `Path` | Übergeben über `args[0].0`. |

### findRootFolders

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "findRootFolders",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="uploadFile" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `destination` | `Path` | Übergeben über `args[0].0`. |
| `fis` | `InputStream` | Übergeben über `args[1].1`. |

### uploadFile

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "uploadFile",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="downloadFile" type="byte[]">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `path` | `Path` | Übergeben über `args[0].0`. |

### downloadFile

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "downloadFile",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="move" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `source` | `Path` | Übergeben über `args[0].0`. |
| `destination` | `Path` | Übergeben über `args[1].1`. |

### move

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "move",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="copy" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `source` | `Path` | Übergeben über `args[0].0`. |
| `destination` | `Path` | Übergeben über `args[1].1`. |

### copy

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "copy",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="rename" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `file` | `Path` | Übergeben über `args[0].0`. |
| `newName` | `String` | Übergeben über `args[1].1`. |

### rename

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "rename",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="delete" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `path` | `Path` | Übergeben über `args[0].0`. |

### delete

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "delete",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="extractZipFile" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `file` | `Path` | Übergeben über `args[0].0`. |
| `destination` | `Path` | Übergeben über `args[1].1`. |

### extractZipFile

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "extractZipFile",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="saveContentToFile" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `name` | `String` | Übergeben über `args[0].0`. |
| `path` | `String` | Übergeben über `args[1].1`. |
| `content` | `String` | Übergeben über `args[2].2`. |

### saveContentToFile

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "saveContentToFile",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="createFile" type="File">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `path` | `Path` | Übergeben über `args[0].0`. |
| `bytes` | `byte[]` | Übergeben über `args[1].1`. |

### createFile

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "createFile",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="setAttributeToFile" type="Path">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `path` | `Path` | Übergeben über `args[0].0`. |
| `name` | `String` | Übergeben über `args[1].1`. |
| `val` | `byte[]` | Übergeben über `args[2].2`. |

### setAttributeToFile

```json
{
  "beanId": "fileManagerServiceMock",
  "scope": "PROTOTYPE",
  "functionName": "setAttributeToFile",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
