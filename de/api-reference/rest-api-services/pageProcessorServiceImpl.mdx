---
title: 'PageProcessorServiceImpl'
description: 'Aufruf öffentlicher Servicemethoden über die REST API: pageProcessorServiceImpl'
---

<ResponseField name="beanId" type="string">

Die Spring-Bean-ID des aufzurufenden Services.

pageProcessorServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Bean-Scope. Für Serviceaufrufe wird PROTOTYPE verwendet.

</ResponseField>

<ResponseField name="functionName" type="string">

Der öffentliche Methodenname des Services, der in der Signatur angegeben ist.

</ResponseField>

<ResponseField name="args" type="object[]">

Das Argument-Array der Methode. Jedes Argument wird mit dem Index 0, 1 usw. übergeben; der Wert muss dem Java-Parametertyp entsprechen.

</ResponseField>

## Headers

```http
POST /api/v1/bean/request
Content-Type: application/json
Lang-Context: de
Site-Context: my-site
Authorization: Bearer <token>
```

Senden Sie `Lang-Context` und `Site-Context` mit jeder Anfrage. Für geschützte Methoden senden Sie das JWT im Header `Authorization` im Format `Bearer <token>`.

**Zugriff:** GUEST. **Bearer Token:** nicht erforderlich.

- GUEST — nicht angemeldete Benutzer; nur Whitelist-Services sind verfügbar.
- USER — Authentifizierung erforderlich; Methoden mit USER-Prüfung sind verfügbar.
- ADMIN — vollständiger Zugriff auf öffentliche Methoden aller Services.
- Wenn eine Methode intern eine Rolle prüft, kann GUEST null erhalten.


## Methoden

<ResponseField name="processPage" type="Map<String,Object>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `pageRoot` | `String` | Übergeben über `args[0].0`. |
| `html` | `String` | Übergeben über `args[1].1`. |

### processPage

```json
{
  "beanId": "pageProcessorServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "processPage",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="processPageTitle" type="Map<String, Object>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `entityId` | `String` | Übergeben über `args[0].0`. |
| `repositoryName` | `String` | Übergeben über `args[1].1`. |

### processPageTitle

```json
{
  "beanId": "pageProcessorServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "processPageTitle",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="processTranslation" type="Map<String, Object>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `pageRoot` | `String` | Übergeben über `args[0].0`. |
| `articleId` | `String` | Übergeben über `args[1].1`. |
| `repository` | `String` | Übergeben über `args[2].2`. |

### processTranslation

```json
{
  "beanId": "pageProcessorServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "processTranslation",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="processMetaData" type="Map<String, Object>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `pageUrl` | `String` | Übergeben über `args[0].0`. |
| `containerId` | `String` | Übergeben über `args[1].1`. |
| `repositoryName` | `String` | Übergeben über `args[2].2`. |

### processMetaData

```json
{
  "beanId": "pageProcessorServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "processMetaData",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="processThemeCss" type="String">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `pageUrl` | `String` | Übergeben über `args[0].0`. |

### processThemeCss

```json
{
  "beanId": "pageProcessorServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "processThemeCss",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
