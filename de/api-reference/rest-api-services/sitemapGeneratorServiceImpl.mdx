---
title: 'SitemapGeneratorServiceImpl'
description: 'Aufruf öffentlicher Servicemethoden über die REST API: sitemapGeneratorServiceImpl'
---

# SitemapGeneratorServiceImpl

<ResponseField name="beanId" type="string">

Die Spring-Bean-ID des aufzurufenden Services.

sitemapGeneratorServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Bean-Scope. Für Serviceaufrufe wird PROTOTYPE verwendet.

</ResponseField>

<ResponseField name="functionName" type="string">

Der öffentliche Methodenname des Services, der in der Signatur angegeben ist.

</ResponseField>

<ResponseField name="args" type="object[]">

Das Argument-Array der Methode. Jedes Argument wird mit dem Index 0, 1 usw. übergeben; der Wert muss dem Java-Parametertyp entsprechen.

</ResponseField>

## Endpunkt und Autorisierung

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Zugriff:** ADMIN. **Bearer Token:** erforderlich.

- GUEST — nicht angemeldete Benutzer; nur Whitelist-Services sind verfügbar.
- USER — Authentifizierung erforderlich; Methoden mit USER-Prüfung sind verfügbar.
- ADMIN — vollständiger Zugriff auf öffentliche Methoden aller Services.
- Wenn eine Methode intern eine Rolle prüft, kann GUEST null erhalten.

## Methoden

<ResponseField name="generateSiteMap" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `siteMapRequest` | `LinkedHashMap<String, Object>` | Übergeben über `args[0].0`. |

### generateSiteMap

```json
{
  "beanId": "sitemapGeneratorServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "generateSiteMap",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getPageLinks" type="HashSet<String>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `baseURL` | `String` | Übergeben über `args[0].0`. |
| `URL` | `String` | Übergeben über `args[1].1`. |
| `links` | `HashSet<String>` | Übergeben über `args[2].2`. |
| `urlsToIgnore` | `List<String>` | Übergeben über `args[3].3`. |

### getPageLinks

```json
{
  "beanId": "sitemapGeneratorServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getPageLinks",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="isWorkInProgress" type="boolean">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| — | — | Diese Methode akzeptiert keine Argumente. |

### isWorkInProgress

```json
{
  "beanId": "sitemapGeneratorServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "isWorkInProgress",
  "args": []
}
```

</ResponseField>

<ResponseField name="changeStatus" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `status` | `ServiceState` | Übergeben über `args[0].0`. |

### changeStatus

```json
{
  "beanId": "sitemapGeneratorServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "changeStatus",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
