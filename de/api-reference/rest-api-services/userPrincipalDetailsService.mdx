---
title: 'UserPrincipalDetailsService'
description: 'Aufruf öffentlicher Servicemethoden über die REST API: userPrincipalDetailsService'
---

# UserPrincipalDetailsService

<ResponseField name="beanId" type="string">

Die Spring-Bean-ID des aufzurufenden Services.

userPrincipalDetailsService

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Bean-Scope. Für Serviceaufrufe wird PROTOTYPE verwendet.

</ResponseField>

<ResponseField name="functionName" type="string">

Der öffentliche Methodenname des Services, der in der Signatur angegeben ist.

</ResponseField>

<ResponseField name="args" type="object[]">

Das Argument-Array der Methode. Jedes Argument wird mit dem Index 0, 1 usw. übergeben; der Wert muss dem Java-Parametertyp entsprechen.

</ResponseField>

## Endpunkt und Autorisierung

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Zugriff:** USER or ADMIN. **Bearer Token:** erforderlich.

- GUEST — nicht angemeldete Benutzer; nur Whitelist-Services sind verfügbar.
- USER — Authentifizierung erforderlich; Methoden mit USER-Prüfung sind verfügbar.
- ADMIN — vollständiger Zugriff auf öffentliche Methoden aller Services.
- Wenn eine Methode intern eine Rolle prüft, kann GUEST null erhalten.


## Methoden

<ResponseField name="loadUserByUsername" type="UserDetails">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `s` | `String` | Übergeben über `args[0].0`. |

### loadUserByUsername

```json
{
  "beanId": "userPrincipalDetailsService",
  "scope": "PROTOTYPE",
  "functionName": "loadUserByUsername",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="loadAuditSettings" type="Optional<UserAuditSettings>">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `id` | `String` | Übergeben über `args[0].0`. |

### loadAuditSettings

```json
{
  "beanId": "userPrincipalDetailsService",
  "scope": "PROTOTYPE",
  "functionName": "loadAuditSettings",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="deleteAuditSettings" type="void">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `id` | `String` | Übergeben über `args[0].0`. |

### deleteAuditSettings

```json
{
  "beanId": "userPrincipalDetailsService",
  "scope": "PROTOTYPE",
  "functionName": "deleteAuditSettings",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="userHasAccessToPage" type="boolean">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `site` | `String` | Übergeben über `args[0].0`. |
| `login` | `String` | Übergeben über `args[1].1`. |
| `requestURI` | `String` | Übergeben über `args[2].2`. |

### userHasAccessToPage

```json
{
  "beanId": "userPrincipalDetailsService",
  "scope": "PROTOTYPE",
  "functionName": "userHasAccessToPage",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="loadRememberMeToken" type="AuthenticationToken">

| Argument | Typ | Beschreibung |
| --- | --- | --- |
| `username` | `String` | Übergeben über `args[0].0`. |

### loadRememberMeToken

```json
{
  "beanId": "userPrincipalDetailsService",
  "scope": "PROTOTYPE",
  "functionName": "loadRememberMeToken",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

