---
title: "msc-catalog"
description: "Editor-Komponente (msc-*)."
---

`msc-catalog` ist eine editor-orientierte Web-Komponente (Lit).

Für diese Komponente gibt es derzeit kein dokumentiertes `msm-*` Äquivalent. Siehe auch [msc-components](./msc-components).

## Usage

```html
<msc-catalog></msc-catalog>
```

