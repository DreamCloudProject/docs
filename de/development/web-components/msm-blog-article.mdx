---
title: "msm-blog-article"
description: "Web-Komponente msm-blog-article."
---

`msm-blog-article` ist eine Web-Komponente (Lit).

## Zweck

Wiederverwendbarer UI-Baustein.

## Beispiel

```html
<msm-blog-article></msm-blog-article>
```

## Attribute / Properties

| Name | Typ | Standard | Hinweise |
| --- | --- | --- | --- |
| `margin-top` | number | `0` |  |
| `margin-bottom` | number | `0` |  |

## Events

Diese Komponente feuert keine eigenen Custom Events.

## CSS parts

Keine `part`-Slots dokumentiert.

## Slots

- `slot[name="date"]`
- `slot[name="title"]`
- `slot[name="text1"]`
- `slot[name="quotation-mark"]`
- `slot[name="quote"]`
- `slot[name="text2"]`

## Methoden

Keine öffentlichen Methoden dokumentiert.
