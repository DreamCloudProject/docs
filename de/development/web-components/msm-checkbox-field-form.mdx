---
title: "msm-checkbox-field-form"
description: "Web-Komponente msm-checkbox-field-form."
---

`msm-checkbox-field-form` ist eine Web-Komponente (Lit).

Formulareingaben und UI-Controls.

```html
<msm-checkbox-field-form></msm-checkbox-field-form>
```

Attribute / Properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `label` | string | - |  |
| `type` | string | - |  |
| `helper` | string | - |  |
| `required` | boolean | - |  |
| `disabled` | boolean | - |  |

Events

Diese Komponente feuert keine eigenen Custom Events.

CSS parts

Keine `part`-Slots dokumentiert.

Slots

Keine Slots dokumentiert.

Methoden

Keine öffentlichen Methoden dokumentiert.
