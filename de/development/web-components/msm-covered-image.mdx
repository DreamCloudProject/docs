---
title: "msm-covered-image"
description: "Web-Komponente msm-covered-image."
---

`msm-covered-image` ist eine Web-Komponente (Lit).

Darstellung von Medien (Bilder/Video).

```html
<msm-covered-image></msm-covered-image>
```

Attribute / Properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `margin-top` | number | `0` |  |
| `margin-bottom` | number | `0` |  |

Events

Diese Komponente feuert keine eigenen Custom Events.

CSS parts

Keine `part`-Slots dokumentiert.

Slots

- `slot[name="content"]`
- `slot[name="image"]`

Methoden

Keine öffentlichen Methoden dokumentiert.
