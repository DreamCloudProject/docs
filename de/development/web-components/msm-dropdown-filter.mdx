---
title: "msm-dropdown-filter"
description: "Web-Komponente msm-dropdown-filter."
---

`msm-dropdown-filter` ist eine Web-Komponente (Lit).

Filter-UI für Listen-/Suchseiten.

```html
<msm-dropdown-filter></msm-dropdown-filter>
```

Attribute / Properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `theme` | string | - |  |
| `filter-id` | string | - |  |
| `filter-type` | string | - |  |
| `name` | string | - |  |
| `disabled` | boolean | - |  |

Events

Diese Komponente feuert keine eigenen Custom Events.

CSS parts

Keine `part`-Slots dokumentiert.

Slots

Keine Slots dokumentiert.

Methoden

Keine öffentlichen Methoden dokumentiert.
