---
title: "msm-group-filter"
description: "Web-Komponente msm-group-filter."
---

`msm-group-filter` ist eine Web-Komponente (Lit).

## Zweck

Filter-UI für Listen-/Suchseiten.

## Beispiel

```html
<msm-group-filter></msm-group-filter>
```

## Attribute / Properties

| Name | Typ | Standard | Hinweise |
| --- | --- | --- | --- |
| `type` | string | - |  |
| `filter-id` | string | - |  |
| `filter-type` | string | - |  |
| `theme` | string | - |  |
| `name` | string | - |  |
| `disabled` | boolean | - |  |
| `visible-attributes` | string | - |  |

## Events

Diese Komponente feuert keine eigenen Custom Events.

## CSS parts

Keine `part`-Slots dokumentiert.

## Slots

Keine Slots dokumentiert.

## Methoden

Keine öffentlichen Methoden dokumentiert.
