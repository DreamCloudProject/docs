---
title: "msm-icon-article"
description: "Web-Komponente msm-icon-article."
---

`msm-icon-article` ist eine Web-Komponente (Lit).

Wiederverwendbarer UI-Baustein.

```html
<msm-icon-article></msm-icon-article>
```

Attribute / Properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `margin-top` | number | `0` |  |
| `margin-bottom` | number | `0` |  |

Events

Diese Komponente feuert keine eigenen Custom Events.

CSS parts

Keine `part`-Slots dokumentiert.

Slots

- `slot[name="image"]`
- `slot[name="text"]`

Methoden

Keine öffentlichen Methoden dokumentiert.
