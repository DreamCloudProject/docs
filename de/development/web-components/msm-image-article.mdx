---
title: "msm-image-article"
description: "Web-Komponente msm-image-article."
---

`msm-image-article` ist eine Web-Komponente (Lit).

Darstellung von Medien (Bilder/Video).

```html
<msm-image-article></msm-image-article>
```

Attribute / Properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `margin-top` | number | `0` |  |
| `margin-bottom` | number | `0` |  |

Events

Diese Komponente feuert keine eigenen Custom Events.

CSS parts

Keine `part`-Slots dokumentiert.

Slots

- `slot[name="title"]`
- `slot[name="heading1"]`
- `slot[name="image"]`
- `slot[name="heading2"]`
- `slot[name="text1"]`
- `slot[name="text2"]`

Methoden

Keine öffentlichen Methoden dokumentiert.
