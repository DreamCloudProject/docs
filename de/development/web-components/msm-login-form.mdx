---
title: "msm-login-form"
description: "Web-Komponente msm-login-form."
---

`msm-login-form` ist eine Web-Komponente (Lit).

UI für Authentifizierung (Login/Logout).

```html
<msm-login-form></msm-login-form>
```

Attribute / Properties

- (none)

Events

Diese Komponente feuert keine eigenen Custom Events.

CSS parts

- `base`

Slots

Keine Slots dokumentiert.

Methoden

Keine öffentlichen Methoden dokumentiert.
