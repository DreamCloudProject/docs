---
title: "msm-main-container"
description: "Web-Komponente msm-main-container."
---

`msm-main-container` ist eine Web-Komponente (Lit).

Wiederverwendbarer UI-Baustein.

```html
<msm-main-container></msm-main-container>
```

Attribute / Properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `padding-top` | number | `0` |  |
| `padding-bottom` | number | `0` |  |

Events

Diese Komponente feuert keine eigenen Custom Events.

CSS parts

Keine `part`-Slots dokumentiert.

Slots

- `slot[name="main-container"]`

Methoden

Keine öffentlichen Methoden dokumentiert.
