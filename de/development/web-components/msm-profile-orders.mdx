---
title: "msm-profile-orders"
description: "Web-Komponente msm-profile-orders."
---

`msm-profile-orders` ist eine Web-Komponente (Lit).

UI-Bausteine für Benutzer/Account.

```html
<msm-profile-orders></msm-profile-orders>
```

Attribute / Properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `href` | string | - |  |
| `scroll-to` | number | - |  |

Events

Diese Komponente feuert keine eigenen Custom Events.

CSS parts

Keine `part`-Slots dokumentiert.

Slots

Keine Slots dokumentiert.

Methoden

Keine öffentlichen Methoden dokumentiert.
