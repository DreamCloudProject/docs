---
title: "msm-review"
description: "Web-Komponente msm-review."
---

`msm-review` ist eine Web-Komponente (Lit).

Wiederverwendbarer UI-Baustein.

```html
<msm-review></msm-review>
```

Attribute / Properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `secret-code-id` | string | - |  |
| `seller-sku-id` | string | - |  |
| `params` | boolean | - |  |
| `reviewObj` | object | - | property only |

Events

Diese Komponente feuert keine eigenen Custom Events.

CSS parts

Keine `part`-Slots dokumentiert.

Slots

Keine Slots dokumentiert.

Methoden

- `validate()`
