---
title: "msm-search-pagination"
description: "Web-Komponente msm-search-pagination."
---

`msm-search-pagination` ist eine Web-Komponente (Lit).

UI für Suche (Eingabe, Filter, Ergebnisse, Pagination).

```html
<msm-search-pagination></msm-search-pagination>
```

Attribute / Properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `type` | string | - |  |
| `scroll-to` | number | - |  |

Events

Diese Komponente feuert keine eigenen Custom Events.

CSS parts

Keine `part`-Slots dokumentiert.

Slots

Keine Slots dokumentiert.

Methoden

Keine öffentlichen Methoden dokumentiert.
