---
title: "msm-select-item"
description: "Web-Komponente msm-select-item."
---

`msm-select-item` ist eine Web-Komponente (Lit).

## Zweck

Formulareingaben und UI-Controls.

## Beispiel

```html
<msm-select-item value="msm-select-item"></msm-select-item>
```

## Attribute / Properties

| Name | Typ | Standard | Hinweise |
| --- | --- | --- | --- |
| `theme` | string | - |  |
| `type` | MenuItemType | - |  |
| `value` | string | - |  |
| `selected` | boolean | - |  |

## Events

| Event | detail | Wann |
| --- | --- | --- |
| `msm-item-change` | `label`, `selected` |  |

### Beispiel

```html
<msm-select-item value="msm-select-item">
</msm-select-item>

<script>
  const el = document.querySelector("msm-select-item");
  el.addEventListener("msm-item-change", (e) => {
    console.log(e.detail);
  });

</script>
```

## CSS parts

- `base`
- `label`
- `prefix`
- `suffix`

## Slots

- `slot[name="prefix"]`
- `slot`
- `slot[name="suffix"]`

## Methoden

- `validate()`

## Hinweise

- Custom events bubble.
