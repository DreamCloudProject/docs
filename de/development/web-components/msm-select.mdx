---
title: "msm-select"
description: "Web-Komponente msm-select."
---

`msm-select` ist eine Web-Komponente (Lit).

## Zweck

Formulareingaben und UI-Controls.

## Beispiel

```html
<msm-select value="msm-select"></msm-select>
```

## Attribute / Properties

| Name | Typ | Standard | Hinweise |
| --- | --- | --- | --- |
| `theme` | string | - |  |
| `type` | MenuItemType | - |  |
| `value` | string | - |  |
| `selected` | boolean | - |  |

## Events

| Event | detail | Wann |
| --- | --- | --- |
| `msm-item-change` | `label`, `selected` |  |

### Beispiel

```html
<msm-select value="msm-select">
</msm-select>

<script>
  const el = document.querySelector("msm-select");
  el.addEventListener("msm-item-change", (e) => {
    console.log(e.detail);
  });

</script>
```

## CSS parts

- `base`
- `label`
- `prefix`
- `suffix`

## Slots

- `slot[name="prefix"]`
- `slot`
- `slot[name="suffix"]`

## Methoden

- `validate()`

## Hinweise

- Custom events bubble.
