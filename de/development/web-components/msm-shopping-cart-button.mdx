---
title: "msm-shopping-cart-button"
description: "Web-Komponente msm-shopping-cart-button."
---

`msm-shopping-cart-button` ist eine Web-Komponente (Lit).

Anzeige und Interaktion mit dem Warenkorb.

```html
<msm-shopping-cart-button></msm-shopping-cart-button>
```

Attribute / Properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `discount-price-id` | string | - |  |
| `z-index` | number | `1` |  |
| `href` | string | - |  |
| `orders-page-url` | string | - |  |
| `order-checkout-page-url` | string | - |  |
| `isOpened` | boolean | - | property only |
| `isLogged` | boolean | - | property only |
| `productPageActive` | boolean | - | property only |
| `theme` | string | - |  |
| `color-scheme` | string | - |  |
| `variant` | string | - |  |
| `display` | string | - |  |
| `behavior` | string | - |  |
| `icon` | string | - |  |
| `icon-height` | string | - |  |

Events

Diese Komponente feuert keine eigenen Custom Events.

CSS parts

- `base`

Slots

Keine Slots dokumentiert.

Methoden

Keine öffentlichen Methoden dokumentiert.
