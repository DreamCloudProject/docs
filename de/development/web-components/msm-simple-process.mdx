---
title: "msm-simple-process"
description: "Web-Komponente msm-simple-process."
---

`msm-simple-process` ist eine Web-Komponente (Lit).

Wiederverwendbarer UI-Baustein.

```html
<msm-simple-process></msm-simple-process>
```

Attribute / Properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `button-link` | string | - |  |
| `button-text` | string | `'Button text'` |  |
| `button-outgoing` | boolean | - |  |
| `margin-top` | number | `0` |  |
| `margin-bottom` | number | `0` |  |

Events

Diese Komponente feuert keine eigenen Custom Events.

CSS parts

Keine `part`-Slots dokumentiert.

Slots

- `slot[name="title"]`
- `slot[name="image1"]`
- `slot[name="heading1"]`
- `slot[name="text1"]`
- `slot[name="image2"]`
- `slot[name="heading2"]`
- `slot[name="text2"]`
- `slot[name="image3"]`
- `slot[name="heading3"]`
- `slot[name="text3"]`

Methoden

Keine öffentlichen Methoden dokumentiert.
