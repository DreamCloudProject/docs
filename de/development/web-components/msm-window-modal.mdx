---
title: "msm-window-modal"
description: "Web-Komponente msm-window-modal."
---

`msm-window-modal` ist eine Web-Komponente (Lit).

Wiederverwendbarer UI-Baustein.

```html
<msm-window-modal></msm-window-modal>
```

Attribute / Properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `z-index` | number | `1` |  |
| `noLink` | boolean | `false` | property only |
| `window` | string | - |  |
| `visible` | boolean | - |  |
| `theme` | string | - |  |
| `object-fit` | string | - |  |
| `color-scheme` | string | - |  |

Events

Diese Komponente feuert keine eigenen Custom Events.

CSS parts

Keine `part`-Slots dokumentiert.

Slots

Keine Slots dokumentiert.

Methoden

Keine öffentlichen Methoden dokumentiert.
