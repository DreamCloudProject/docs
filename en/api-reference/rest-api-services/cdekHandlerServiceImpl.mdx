---
title: 'CdekHandlerServiceImpl'
description: 'Calling public service methods through the REST API: cdekHandlerServiceImpl'
---

# CdekHandlerServiceImpl

<ResponseField name="beanId" type="string">

The Spring bean identifier containing the service being called.

cdekHandlerServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Bean scope. Use PROTOTYPE for service calls.

</ResponseField>

<ResponseField name="functionName" type="string">

The public service method name shown in the signature below.

</ResponseField>

<ResponseField name="args" type="object[]">

The method argument array. Each argument is passed as an object with index 0, 1, and so on; the value must match the Java parameter type.

</ResponseField>

## Endpoint and authorization

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Access:** ADMIN. **Bearer Token:** required.

- GUEST — unauthenticated users; only whitelist services are available.
- USER — authentication is required; methods with USER checks are available.
- ADMIN — full access to public methods of all services.
- If a method checks a role internally, GUEST may receive null.

## Methods

<ResponseField name="handleDeliveryPoints" type="Object">

| Argument | Type | Description |
| --- | --- | --- |
| `request` | `Map<String,Object>` | Passed through `args[0].0`. |

### handleDeliveryPoints

```json
{
  "beanId": "cdekHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleDeliveryPoints",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handleCalculateDelivery" type="Object">

| Argument | Type | Description |
| --- | --- | --- |
| `request` | `Map<String, Object>` | Passed through `args[0].0`. |

### handleCalculateDelivery

```json
{
  "beanId": "cdekHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleCalculateDelivery",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handleTariffs" type="Object">

| Argument | Type | Description |
| --- | --- | --- |
| `request` | `Map<String, Object>` | Passed through `args[0].0`. |

### handleTariffs

```json
{
  "beanId": "cdekHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleTariffs",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handleWebhook" type="Object">

| Argument | Type | Description |
| --- | --- | --- |
| `request` | `Map<String, Object>` | Passed through `args[0].0`. |

### handleWebhook

```json
{
  "beanId": "cdekHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleWebhook",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
