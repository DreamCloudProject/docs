---
title: 'ecomSearchIndexServiceImpl'
description: 'Calling public service methods through the REST API: ecomSearchIndexServiceImpl'
---

# ecomSearchIndexServiceImpl

<ResponseField name="beanId" type="string">

The Spring bean identifier containing the service being called.

ecomSearchIndexServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Bean scope. Use PROTOTYPE for service calls.

</ResponseField>

<ResponseField name="functionName" type="string">

The public service method name shown in the signature below.

</ResponseField>

<ResponseField name="args" type="object[]">

The method argument array. Each argument is passed as an object with index 0, 1, and so on; the value must match the Java parameter type.

</ResponseField>

## Endpoint and authorization

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Access:** ADMIN. **Bearer Token:** required.

- GUEST — unauthenticated users; only whitelist services are available.
- USER — authentication is required; methods with USER checks are available.
- ADMIN — full access to public methods of all services.
- If a method checks a role internally, GUEST may receive null.

## Methods

<ResponseField name="generateKeywordsForSKUs" type="void">

| Argument | Type | Description |
| --- | --- | --- |
| `ids` | `ArrayList<String>` | Passed through `args[0].0`. |

### generateKeywordsForSKUs

```json
{
  "beanId": "ecomSearchIndexServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "generateKeywordsForSKUs",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="removeKeywordsForSKUs" type="void">

| Argument | Type | Description |
| --- | --- | --- |
| `ids` | `ArrayList<String>` | Passed through `args[0].0`. |

### removeKeywordsForSKUs

```json
{
  "beanId": "ecomSearchIndexServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "removeKeywordsForSKUs",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="generateSellerSkuForSeller" type="void">

| Argument | Type | Description |
| --- | --- | --- |
| `sellerId` | `String` | Passed through `args[0].0`. |

### generateSellerSkuForSeller

```json
{
  "beanId": "ecomSearchIndexServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "generateSellerSkuForSeller",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="removeSellerSkuForSeller" type="void">

| Argument | Type | Description |
| --- | --- | --- |
| `sellerId` | `String` | Passed through `args[0].0`. |

### removeSellerSkuForSeller

```json
{
  "beanId": "ecomSearchIndexServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "removeSellerSkuForSeller",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
