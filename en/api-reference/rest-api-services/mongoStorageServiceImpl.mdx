---
title: 'MongoStorageServiceImpl'
description: 'Calling public service methods through the REST API: mongoStorageServiceImpl'
---

# MongoStorageServiceImpl

<ResponseField name="beanId" type="string">

The Spring bean identifier containing the service being called.

mongoStorageServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Bean scope. Use PROTOTYPE for service calls.

</ResponseField>

<ResponseField name="functionName" type="string">

The public service method name shown in the signature below.

</ResponseField>

<ResponseField name="args" type="object[]">

The method argument array. Each argument is passed as an object with index 0, 1, and so on; the value must match the Java parameter type.

</ResponseField>

## Endpoint and authorization

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Access:** ADMIN. **Bearer Token:** required.

- GUEST — unauthenticated users; only whitelist services are available.
- USER — authentication is required; methods with USER checks are available.
- ADMIN — full access to public methods of all services.
- If a method checks a role internally, GUEST may receive null.


## Methods

<ResponseField name="getCollection" type="MongoCollection<Document>">

| Argument | Type | Description |
| --- | --- | --- |
| `dbName` | `String` | Passed through `args[0].0`. |
| `collectionName` | `String` | Passed through `args[1].1`. |

### getCollection

```json
{
  "beanId": "mongoStorageServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getCollection",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getCollections" type="List<Map<String, String>>">

| Argument | Type | Description |
| --- | --- | --- |
| `dbName` | `String` | Passed through `args[0].0`. |

### getCollections

```json
{
  "beanId": "mongoStorageServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getCollections",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getDatabase" type="MongoDatabase">

| Argument | Type | Description |
| --- | --- | --- |
| `dbName` | `String` | Passed through `args[0].0`. |

### getDatabase

```json
{
  "beanId": "mongoStorageServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getDatabase",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="removeDocument" type="void">

| Argument | Type | Description |
| --- | --- | --- |
| `dbName` | `String` | Passed through `args[0].0`. |
| `collectionName` | `String` | Passed through `args[1].1`. |
| `id` | `String` | Passed through `args[2].2`. |

### removeDocument

```json
{
  "beanId": "mongoStorageServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "removeDocument",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="downloadBinary" type="byte[]">

| Argument | Type | Description |
| --- | --- | --- |
| `id` | `String` | Passed through `args[0].0`. |

### downloadBinary

```json
{
  "beanId": "mongoStorageServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "downloadBinary",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getBinaryStorage" type="GridFSBucket">

| Argument | Type | Description |
| --- | --- | --- |
| `dbName` | `String` | Passed through `args[0].0`. |
| `binaryCollection` | `String` | Passed through `args[1].1`. |

### getBinaryStorage

```json
{
  "beanId": "mongoStorageServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getBinaryStorage",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="removeBinary" type="void">

| Argument | Type | Description |
| --- | --- | --- |
| `dbName` | `String` | Passed through `args[0].0`. |
| `binStorageName` | `String` | Passed through `args[1].1`. |
| `json` | `String` | Passed through `args[2].2`. |

### removeBinary

```json
{
  "beanId": "mongoStorageServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "removeBinary",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="removeBinary" type="void">

| Argument | Type | Description |
| --- | --- | --- |
| `id` | `String` | Passed through `args[0].0`. |

### removeBinary

```json
{
  "beanId": "mongoStorageServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "removeBinary",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

