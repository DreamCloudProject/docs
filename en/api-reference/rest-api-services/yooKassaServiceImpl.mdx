---
title: 'yooKassaServiceImpl'
description: 'Calling public service methods through the REST API: yooKassaServiceImpl'
---

# yooKassaServiceImpl

<ResponseField name="beanId" type="string">

The Spring bean identifier containing the service being called.

yooKassaServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Bean scope. Use PROTOTYPE for service calls.

</ResponseField>

<ResponseField name="functionName" type="string">

The public service method name shown in the signature below.

</ResponseField>

<ResponseField name="args" type="object[]">

The method argument array. Each argument is passed as an object with index 0, 1, and so on; the value must match the Java parameter type.

</ResponseField>

## Endpoint and authorization

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Access:** ADMIN. **Bearer Token:** required.

- GUEST — unauthenticated users; only whitelist services are available.
- USER — authentication is required; methods with USER checks are available.
- ADMIN — full access to public methods of all services.
- If a method checks a role internally, GUEST may receive null.

## Methods

<ResponseField name="init" type="void">

| Argument | Type | Description |
| --- | --- | --- |
| — | — | This method does not accept arguments. |

### init

```json
{
  "beanId": "yooKassaServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "init",
  "args": []
}
```

</ResponseField>

<ResponseField name="isYooKassaKeysDefined" type="boolean">

| Argument | Type | Description |
| --- | --- | --- |
| — | — | This method does not accept arguments. |

### isYooKassaKeysDefined

```json
{
  "beanId": "yooKassaServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "isYooKassaKeysDefined",
  "args": []
}
```

</ResponseField>

<ResponseField name="createPayment" type="Payment">

| Argument | Type | Description |
| --- | --- | --- |
| `order` | `OrderDTO` | Passed through `args[0].0`. |

### createPayment

```json
{
  "beanId": "yooKassaServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "createPayment",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="chargePayment" type="PaymentStatus">

| Argument | Type | Description |
| --- | --- | --- |
| `paymentId` | `String` | Passed through `args[0].0`. |

### chargePayment

```json
{
  "beanId": "yooKassaServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "chargePayment",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="cancelPayment" type="PaymentStatus">

| Argument | Type | Description |
| --- | --- | --- |
| `paymentId` | `String` | Passed through `args[0].0`. |

### cancelPayment

```json
{
  "beanId": "yooKassaServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "cancelPayment",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="refundPayment" type="PaymentStatus">

| Argument | Type | Description |
| --- | --- | --- |
| `order` | `OrderDTO` | Passed through `args[0].0`. |

### refundPayment

```json
{
  "beanId": "yooKassaServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "refundPayment",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="payoutToVendor" type="Payout">

| Argument | Type | Description |
| --- | --- | --- |
| `vendor` | `Seller` | Passed through `args[0].0`. |
| `orderId` | `String` | Passed through `args[1].1`. |
| `amount` | `long` | Passed through `args[2].2`. |

### payoutToVendor

```json
{
  "beanId": "yooKassaServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "payoutToVendor",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
