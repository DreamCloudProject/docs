---
title: "msm:empty"
description: "Fallback markup when there is no data"
---

`msm:empty` defines fallback markup when there is nothing to render.

Commonly used inside list-like components such as `msm:foreach` and `msm:rest`.

