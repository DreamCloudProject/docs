---
title: "msm:template"
description: "Main template used by SSR components"
---

`msm:template` defines the main markup to render when data is present.

It is used inside SSR components like `msm:block`, `msm:foreach`, `msm:rest`, `msm:if`, `msm:select`, and `msm:tree`.

