---
title: "msc-condition-handler"
description: "Editor component (msc-*)."
---

`msc-condition-handler` is an editor-focused web component (Lit).

This component currently has no documented `msm-*` equivalent. See also [msc-components](./msc-components).

## Usage

```html
<msc-condition-handler></msc-condition-handler>
```

