---
title: "msc-listing-feature-csr"
description: "Editor component (msc-*)."
---

`msc-listing-feature-csr` is an editor-focused web component (Lit).

This component currently has no documented `msm-*` equivalent. See also [msc-components](./msc-components).

## Usage

```html
<msc-listing-feature-csr></msc-listing-feature-csr>
```

