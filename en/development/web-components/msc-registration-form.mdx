---
title: "msc-registration-form"
description: "Editor component (msc-*)."
---

`msc-registration-form` is an editor-focused web component (Lit).

This component currently has no documented `msm-*` equivalent. See also [msc-components](./msc-components).

## Usage

```html
<msc-registration-form></msc-registration-form>
```

