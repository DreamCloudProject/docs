---
title: "msm-bulletpoint"
description: "Web component msm-bulletpoint."
---

`msm-bulletpoint` is a web component built with Lit.

Use this component as a reusable UI building block.

```html
<msm-bulletpoint></msm-bulletpoint>
```

Attributes / properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `button-link` | string | - |  |
| `button-text` | string | `'This is a link, that takes you to some place.'` |  |
| `button-outgoing` | boolean | - |  |
| `margin-top` | number | `0` |  |
| `margin-bottom` | number | `0` |  |

Events

This component does not emit custom events.

CSS parts

No documented `part` slots.

Slots

No documented slots.

Methods

No documented public methods.
