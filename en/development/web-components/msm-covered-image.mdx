---
title: "msm-covered-image"
description: "Web component msm-covered-image."
---

`msm-covered-image` is a web component built with Lit.

Render media content (images/video).

```html
<msm-covered-image></msm-covered-image>
```

Attributes / properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `margin-top` | number | `0` |  |
| `margin-bottom` | number | `0` |  |

Events

This component does not emit custom events.

CSS parts

No documented `part` slots.

Slots

- `slot[name="content"]`
- `slot[name="image"]`

Methods

No documented public methods.
