---
title: "msm-dropdown-filter"
description: "Filter UI component."
---

`msm-dropdown-filter` is a web component built with Lit.

Render filter UI for listing/search pages.

```html
<msm-dropdown-filter></msm-dropdown-filter>
```

Attributes / properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `theme` | string | - |  |
| `filter-id` | string | - |  |
| `filter-type` | string | - |  |
| `name` | string | - |  |
| `disabled` | boolean | - |  |

Events

This component does not emit custom events.

CSS parts

No documented `part` slots.

Slots

No documented slots.

Methods

No documented public methods.
