---
title: "msm-enumeration"
description: "Web component msm-enumeration."
---

`msm-enumeration` is a web component built with Lit.

Use this component as a reusable UI building block.

```html
<msm-enumeration></msm-enumeration>
```

Attributes / properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `margin-top` | number | `0` |  |
| `margin-bottom` | number | `0` |  |

Events

This component does not emit custom events.

CSS parts

- `base`
- `step`

Slots

- `slot[name="number"]`
- `slot[name="text"]`

Methods

No documented public methods.
