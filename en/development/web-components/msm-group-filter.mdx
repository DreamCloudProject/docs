---
title: "msm-group-filter"
description: "Filter UI component."
---

`msm-group-filter` is a web component built with Lit.

Render filter UI for listing/search pages.

```html
<msm-group-filter></msm-group-filter>
```

Attributes / properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `type` | string | - |  |
| `filter-id` | string | - |  |
| `filter-type` | string | - |  |
| `theme` | string | - |  |
| `name` | string | - |  |
| `disabled` | boolean | - |  |
| `visible-attributes` | string | - |  |

Events

This component does not emit custom events.

CSS parts

No documented `part` slots.

Slots

No documented slots.

Methods

No documented public methods.
