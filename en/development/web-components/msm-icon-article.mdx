---
title: "msm-icon-article"
description: "Web component msm-icon-article."
---

`msm-icon-article` is a web component built with Lit.

Use this component as a reusable UI building block.

```html
<msm-icon-article></msm-icon-article>
```

Attributes / properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `margin-top` | number | `0` |  |
| `margin-bottom` | number | `0` |  |

Events

This component does not emit custom events.

CSS parts

No documented `part` slots.

Slots

- `slot[name="image"]`
- `slot[name="text"]`

Methods

No documented public methods.
