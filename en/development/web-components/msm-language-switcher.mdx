---
title: "msm-language-switcher"
description: "Web component msm-language-switcher."
---

`msm-language-switcher` is a web component built with Lit.

Use this component as a reusable UI building block.

```html
<msm-language-switcher></msm-language-switcher>
```

Attributes / properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `theme` | string | `Theme.fromValue(this.theme).name` |  |
| `type` | string | `this.type ?` |  |

Events

This component does not emit custom events.

CSS parts

No documented `part` slots.

Slots

No documented slots.

Methods

No documented public methods.
