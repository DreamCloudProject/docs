---
title: "msm-select-item"
description: "Web component msm-select-item."
---

`msm-select-item` is a web component built with Lit.

## Purpose

Render form inputs and UI controls.

## Usage

```html
<msm-select-item value="msm-select-item"></msm-select-item>
```

## Attributes / properties

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `theme` | string | - |  |
| `type` | MenuItemType | - |  |
| `value` | string | - |  |
| `selected` | boolean | - |  |

## Events

| Event | detail | When |
| --- | --- | --- |
| `msm-item-change` | `label`, `selected` |  |

### Usage

```html
<msm-select-item value="msm-select-item">
</msm-select-item>

<script>
  const el = document.querySelector("msm-select-item");
  el.addEventListener("msm-item-change", (e) => {
    console.log(e.detail);
  });

</script>
```

## CSS parts

- `base`
- `label`
- `prefix`
- `suffix`

## Slots

- `slot[name="prefix"]`
- `slot`
- `slot[name="suffix"]`

## Methods

- `validate()`

## Notes

- Custom events bubble.
