---
title: "msm-telephone-field"
description: "Web component msm-telephone-field."
---

`msm-telephone-field` is a web component built with Lit.

Render form inputs and UI controls.

```html
<msm-telephone-field></msm-telephone-field>
```

Attributes / properties

- (none)

Events

This component does not emit custom events.

CSS parts

- `base`
- `frame`
- `helper`
- `input`
- `label`
- `notch`

Slots

- `slot[name="button"]`

Methods

- `validate()`
