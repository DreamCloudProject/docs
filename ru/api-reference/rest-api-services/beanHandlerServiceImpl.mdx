---
title: 'BeanHandlerServiceImpl'
description: 'Вызов публичных методов сервиса через REST API: beanHandlerServiceImpl'
---

# BeanHandlerServiceImpl

<ResponseField name="beanId" type="string">

Идентификатор Spring bean, который содержит вызываемый сервис.

beanHandlerServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Область действия bean. Для вызовов сервисов используется PROTOTYPE.

</ResponseField>

<ResponseField name="functionName" type="string">

Имя публичного метода сервиса, указанное в сигнатуре ниже.

</ResponseField>

<ResponseField name="args" type="object[]">

Массив аргументов метода. Каждый аргумент передаётся объектом с индексом 0, 1 и т. д.; значение должно соответствовать Java-типу параметра.

</ResponseField>

## Endpoint и авторизация

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Доступ:** ADMIN. **Bearer Token:** требуется.

- GUEST — без логина; доступен только whitelist.
- USER — требуется авторизация; доступны методы с проверкой USER.
- ADMIN — полный доступ к public-методам всех сервисов.
- Если метод проверяет роль внутри логики, GUEST может получить null.

## Методы

<ResponseField name="getRepository" type="Object">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `repositoryId` | `String` | Передаётся через `args[0].0`. |

### getRepository

```json
{
  "beanId": "beanHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getRepository",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getBean" type="Object">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `beanId` | `String` | Передаётся через `args[0].0`. |

### getBean

```json
{
  "beanId": "beanHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getBean",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getSessionBean" type="Object">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `beanId` | `String` | Передаётся через `args[0].0`. |

### getSessionBean

```json
{
  "beanId": "beanHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getSessionBean",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="updateSessionBean" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `beanId` | `String` | Передаётся через `args[0].0`. |
| `bean` | `Object` | Передаётся через `args[1].1`. |

### updateSessionBean

```json
{
  "beanId": "beanHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "updateSessionBean",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handleUpdate" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `BeanRequestMessage` | Передаётся через `args[0].0`. |

### handleUpdate

```json
{
  "beanId": "beanHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleUpdate",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handleInvoke" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `BeanRequestMessage` | Передаётся через `args[0].0`. |

### handleInvoke

```json
{
  "beanId": "beanHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleInvoke",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handleRepositoryCall" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `RepositoryRequestMessage` | Передаётся через `args[0].0`. |

### handleRepositoryCall

```json
{
  "beanId": "beanHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleRepositoryCall",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
