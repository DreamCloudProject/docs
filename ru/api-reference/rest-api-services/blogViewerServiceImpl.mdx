---
title: 'BlogViewerServiceImpl'
description: 'Вызов публичных методов сервиса через REST API: blogViewerServiceImpl'
---

# BlogViewerServiceImpl

<ResponseField name="beanId" type="string">

Идентификатор Spring bean, который содержит вызываемый сервис.

blogViewerServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Область действия bean. Для вызовов сервисов используется PROTOTYPE.

</ResponseField>

<ResponseField name="functionName" type="string">

Имя публичного метода сервиса, указанное в сигнатуре ниже.

</ResponseField>

<ResponseField name="args" type="object[]">

Массив аргументов метода. Каждый аргумент передаётся объектом с индексом 0, 1 и т. д.; значение должно соответствовать Java-типу параметра.

</ResponseField>

## Endpoint и авторизация

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Доступ:** GUEST. **Bearer Token:** не требуется.

- GUEST — без логина; доступен только whitelist.
- USER — требуется авторизация; доступны методы с проверкой USER.
- ADMIN — полный доступ к public-методам всех сервисов.
- Если метод проверяет роль внутри логики, GUEST может получить null.


## Методы

<ResponseField name="getArticles" type="List<Map<String, Object>>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `regexFilter` | `String` | Передаётся через `args[0].0`. |

### getArticles

```json
{
  "beanId": "blogViewerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getArticles",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getContainerArticles" type="List<Map<String, String>>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `blogId` | `String` | Передаётся через `args[0].0`. |

### getContainerArticles

```json
{
  "beanId": "blogViewerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getContainerArticles",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getRelatedBlogs" type="List<Map<String, Object>>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `blogId` | `String` | Передаётся через `args[0].0`. |
| `size` | `String` | Передаётся через `args[1].1`. |

### getRelatedBlogs

```json
{
  "beanId": "blogViewerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getRelatedBlogs",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="isContainerTypeEqualsTo" type="boolean">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `containerId` | `String` | Передаётся через `args[0].0`. |
| `type` | `String` | Передаётся через `args[1].1`. |

### isContainerTypeEqualsTo

```json
{
  "beanId": "blogViewerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "isContainerTypeEqualsTo",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

