---
title: 'documentHandlerServiceImpl'
description: 'Вызов публичных методов сервиса через REST API: documentHandlerServiceImpl'
---

# documentHandlerServiceImpl

<ResponseField name="beanId" type="string">

Идентификатор Spring bean, который содержит вызываемый сервис.

documentHandlerServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Область действия bean. Для вызовов сервисов используется PROTOTYPE.

</ResponseField>

<ResponseField name="functionName" type="string">

Имя публичного метода сервиса, указанное в сигнатуре ниже.

</ResponseField>

<ResponseField name="args" type="object[]">

Массив аргументов метода. Каждый аргумент передаётся объектом с индексом 0, 1 и т. д.; значение должно соответствовать Java-типу параметра.

</ResponseField>

## Endpoint и авторизация

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Доступ:** ADMIN. **Bearer Token:** требуется.

- GUEST — без логина; доступен только whitelist.
- USER — требуется авторизация; доступны методы с проверкой USER.
- ADMIN — полный доступ к public-методам всех сервисов.
- Если метод проверяет роль внутри логики, GUEST может получить null.

## Методы

<ResponseField name="handleAddDocument" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `DocumentRequestMessage` | Передаётся через `args[0].0`. |

### handleAddDocument

```json
{
  "beanId": "documentHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleAddDocument",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handleRemoveDocument" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `DocumentRequestMessage` | Передаётся через `args[0].0`. |

### handleRemoveDocument

```json
{
  "beanId": "documentHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleRemoveDocument",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handleFindDocumentResponse" type="Map<String, Object>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `DocumentRequestMessage` | Передаётся через `args[0].0`. |

### handleFindDocumentResponse

```json
{
  "beanId": "documentHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleFindDocumentResponse",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handleAddDocumentResponse" type="Map<String, Object>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `DocumentRequestMessage` | Передаётся через `args[0].0`. |

### handleAddDocumentResponse

```json
{
  "beanId": "documentHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleAddDocumentResponse",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handleRemoveDocumentResponse" type="Map<String, Object>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `DocumentRequestMessage` | Передаётся через `args[0].0`. |

### handleRemoveDocumentResponse

```json
{
  "beanId": "documentHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleRemoveDocumentResponse",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
