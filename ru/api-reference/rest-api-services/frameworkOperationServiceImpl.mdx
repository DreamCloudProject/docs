---
title: 'FrameworkOperationServiceImpl'
description: 'Вызов публичных методов сервиса через REST API: frameworkOperationServiceImpl'
---

<ResponseField name="beanId" type="string">

Идентификатор Spring bean, который содержит вызываемый сервис.

frameworkOperationServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Область действия bean. Для вызовов сервисов используется PROTOTYPE.

</ResponseField>

<ResponseField name="functionName" type="string">

Имя публичного метода сервиса, указанное в сигнатуре ниже.

</ResponseField>

<ResponseField name="args" type="object[]">

Массив аргументов метода. Каждый аргумент передаётся объектом с индексом 0, 1 и т. д.; значение должно соответствовать Java-типу параметра.

</ResponseField>

## Headers

```http
POST /api/v1/bean/request
Content-Type: application/json
Lang-Context: ru
Site-Context: my-site
Authorization: Bearer <token>
```

Заголовки `Lang-Context` и `Site-Context` необходимо передавать в каждом запросе. Для защищённых методов передавайте JWT в заголовке `Authorization` в формате `Bearer <token>`.

**Доступ:** GUEST. **Bearer Token:** не требуется.

- GUEST — без логина; доступен только whitelist.
- USER — требуется авторизация; доступны методы с проверкой USER.
- ADMIN — полный доступ к public-методам всех сервисов.
- Если метод проверяет роль внутри логики, GUEST может получить null.


## Методы

<ResponseField name="isValueEquals" type="boolean">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `val` | `String` | Передаётся через `args[0].0`. |
| `equalsToVal` | `String` | Передаётся через `args[1].1`. |

### isValueEquals

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "isValueEquals",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="isNotBlank" type="boolean">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `value` | `String` | Передаётся через `args[0].0`. |

### isNotBlank

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "isNotBlank",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="isBlank" type="boolean">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `value` | `String` | Передаётся через `args[0].0`. |

### isBlank

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "isBlank",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="isNotTrue" type="boolean">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `value` | `String` | Передаётся через `args[0].0`. |

### isNotTrue

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "isNotTrue",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="isTrue" type="boolean">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `value` | `String` | Передаётся через `args[0].0`. |

### isTrue

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "isTrue",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="isFileExists" type="boolean">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `path` | `String` | Передаётся через `args[0].0`. |

### isFileExists

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "isFileExists",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="isFileExistsFromSystemRoot" type="boolean">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `path` | `String` | Передаётся через `args[0].0`. |

### isFileExistsFromSystemRoot

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "isFileExistsFromSystemRoot",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="isFileExistsFromUploadRoot" type="boolean">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `path` | `String` | Передаётся через `args[0].0`. |

### isFileExistsFromUploadRoot

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "isFileExistsFromUploadRoot",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="isLanguageDetection" type="boolean">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| — | — | Метод не принимает аргументы. |

### isLanguageDetection

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "isLanguageDetection",
  "args": []
}
```

</ResponseField>

<ResponseField name="getActiveLanguages" type="List<String>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| — | — | Метод не принимает аргументы. |

### getActiveLanguages

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getActiveLanguages",
  "args": []
}
```

</ResponseField>

<ResponseField name="getTimeFromDate" type="Map<String, String>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `date` | `String` | Передаётся через `args[0].0`. |

### getTimeFromDate

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getTimeFromDate",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getCurrentDate" type="Map<String, String>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `pattern` | `String` | Передаётся через `args[0].0`. |

### getCurrentDate

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getCurrentDate",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getFormattedDate" type="Map<String, String>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `date` | `String` | Передаётся через `args[0].0`. |
| `pattern` | `String` | Передаётся через `args[1].1`. |

### getFormattedDate

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getFormattedDate",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getFormattedDateFromTimestamp" type="Map<String, String>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `timestamp` | `String` | Передаётся через `args[0].0`. |
| `pattern` | `String` | Передаётся через `args[1].1`. |

### getFormattedDateFromTimestamp

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getFormattedDateFromTimestamp",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getDaysTillEndDate" type="Map<String, Object>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `date` | `String` | Передаётся через `args[0].0`. |

### getDaysTillEndDate

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getDaysTillEndDate",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getDatePlusYears" type="Map<String, Object>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `years` | `String` | Передаётся через `args[0].0`. |
| `pattern` | `String` | Передаётся через `args[1].1`. |

### getDatePlusYears

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getDatePlusYears",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getDaysTillEndDateFromTimestamp" type="Map<String, Object>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `timestamp` | `String` | Передаётся через `args[0].0`. |

### getDaysTillEndDateFromTimestamp

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getDaysTillEndDateFromTimestamp",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="multiplyValueTo" type="Map<String, Object>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `val` | `String` | Передаётся через `args[0].0`. |
| `to` | `String` | Передаётся через `args[1].1`. |

### multiplyValueTo

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "multiplyValueTo",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="divideValueTo" type="Map<String, Object>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `val` | `String` | Передаётся через `args[0].0`. |
| `to` | `String` | Передаётся через `args[1].1`. |

### divideValueTo

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "divideValueTo",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="plusTwoValues" type="Map<String, Object>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `val1` | `String` | Передаётся через `args[0].0`. |
| `val2` | `String` | Передаётся через `args[1].1`. |

### plusTwoValues

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "plusTwoValues",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getFileSize" type="Map<String, String>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `url` | `String` | Передаётся через `args[0].0`. |
| `dim` | `String` | Передаётся через `args[1].1`. |

### getFileSize

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getFileSize",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getFileExtension" type="Map<String, String>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `filePath` | `String` | Передаётся через `args[0].0`. |

### getFileExtension

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getFileExtension",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getFirstLetter" type="Map<String, String>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `text` | `String` | Передаётся через `args[0].0`. |

### getFirstLetter

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getFirstLetter",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getCurrentSequence" type="Map<String, String>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `seqName` | `String` | Передаётся через `args[0].0`. |

### getCurrentSequence

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getCurrentSequence",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="transformJsonArrayToList" type="List<Map<String,Object>>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `jsonArray` | `String` | Передаётся через `args[0].0`. |

### transformJsonArrayToList

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "transformJsonArrayToList",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getLangCode" type="String">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| — | — | Метод не принимает аргументы. |

### getLangCode

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getLangCode",
  "args": []
}
```

</ResponseField>

<ResponseField name="toLowerCase" type="Map<String, String>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `str` | `String` | Передаётся через `args[0].0`. |

### toLowerCase

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "toLowerCase",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="toUpperCase" type="Map<String, String>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `str` | `String` | Передаётся через `args[0].0`. |

### toUpperCase

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "toUpperCase",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="restClientCall" type="Object">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `endpoint` | `String` | Передаётся через `args[0].0`. |
| `method` | `String` | Передаётся через `args[1].1`. |
| `jsonAsString` | `String` | Передаётся через `args[2].2`. |

### restClientCall

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "restClientCall",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="parseNestedDatabaseDocs" type="HashMap<Object, Object>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `itemName` | `String` | Передаётся через `args[0].0`. |
| `nestedKey` | `String` | Передаётся через `args[1].1`. |
| `database` | `String` | Передаётся через `args[2].2`. |
| `jsonObjString` | `String` | Передаётся через `args[3].3`. |

### parseNestedDatabaseDocs

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "parseNestedDatabaseDocs",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="languageAwareUrl" type="String">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `url` | `String` | Передаётся через `args[0].0`. |

### languageAwareUrl

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "languageAwareUrl",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getCurrentWebSiteSettings" type="WebSiteSettings">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| — | — | Метод не принимает аргументы. |

### getCurrentWebSiteSettings

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getCurrentWebSiteSettings",
  "args": []
}
```

</ResponseField>

<ResponseField name="getCurrentBrandingSiteSettings" type="LinkedHashMap<String, Object>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| — | — | Метод не принимает аргументы. |

### getCurrentBrandingSiteSettings

```json
{
  "beanId": "frameworkOperationServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getCurrentBrandingSiteSettings",
  "args": []
}
```

</ResponseField>
