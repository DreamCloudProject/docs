---
title: 'machineLearningServiceImpl'
description: 'Вызов публичных методов сервиса через REST API: machineLearningServiceImpl'
---

# machineLearningServiceImpl

<ResponseField name="beanId" type="string">

Идентификатор Spring bean, который содержит вызываемый сервис.

machineLearningServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Область действия bean. Для вызовов сервисов используется PROTOTYPE.

</ResponseField>

<ResponseField name="functionName" type="string">

Имя публичного метода сервиса, указанное в сигнатуре ниже.

</ResponseField>

<ResponseField name="args" type="object[]">

Массив аргументов метода. Каждый аргумент передаётся объектом с индексом 0, 1 и т. д.; значение должно соответствовать Java-типу параметра.

</ResponseField>

## Endpoint и авторизация

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Доступ:** ADMIN. **Bearer Token:** требуется.

- GUEST — без логина; доступен только whitelist.
- USER — требуется авторизация; доступны методы с проверкой USER.
- ADMIN — полный доступ к public-методам всех сервисов.
- Если метод проверяет роль внутри логики, GUEST может получить null.

## Методы

<ResponseField name="recognizeAttributesForSellerSku" type="Map<String,Object>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `urls` | `ArrayList<String>` | Передаётся через `args[0].0`. |
| `sellerSkuType` | `String` | Передаётся через `args[1].1`. |
| `isAbsolutePath` | `Boolean` | Передаётся через `args[2].2`. |

### recognizeAttributesForSellerSku

```json
{
  "beanId": "machineLearningServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "recognizeAttributesForSellerSku",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="generateDatasetForAwsComprehend" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `filePath` | `String` | Передаётся через `args[0].0`. |
| `langCode` | `String` | Передаётся через `args[1].1`. |
| `targetDb` | `String` | Передаётся через `args[2].2`. |
| `targetCollection` | `String` | Передаётся через `args[3].3`. |
| `isAbsolutePath` | `Boolean` | Передаётся через `args[4].4`. |

### generateDatasetForAwsComprehend

```json
{
  "beanId": "machineLearningServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "generateDatasetForAwsComprehend",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="generateTranslationsForLanguage" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `fromLangIsoCode` | `String` | Передаётся через `args[0].0`. |
| `toLangIsoCode` | `String` | Передаётся через `args[1].1`. |

### generateTranslationsForLanguage

```json
{
  "beanId": "machineLearningServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "generateTranslationsForLanguage",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="translateDatabaseForLanguage" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `fromLangIsoCode` | `String` | Передаётся через `args[0].0`. |
| `toLangIsoCode` | `String` | Передаётся через `args[1].1`. |
| `targetDb` | `String` | Передаётся через `args[2].2`. |
| `targetCollection` | `String` | Передаётся через `args[3].3`. |
| `fieldNames` | `ArrayList<String>` | Передаётся через `args[4].4`. |

### translateDatabaseForLanguage

```json
{
  "beanId": "machineLearningServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "translateDatabaseForLanguage",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
