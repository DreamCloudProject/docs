---
title: 'sitemapGeneratorServiceImpl'
description: 'Вызов публичных методов сервиса через REST API: sitemapGeneratorServiceImpl'
---

# sitemapGeneratorServiceImpl

<ResponseField name="beanId" type="string">

Идентификатор Spring bean, который содержит вызываемый сервис.

sitemapGeneratorServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Область действия bean. Для вызовов сервисов используется PROTOTYPE.

</ResponseField>

<ResponseField name="functionName" type="string">

Имя публичного метода сервиса, указанное в сигнатуре ниже.

</ResponseField>

<ResponseField name="args" type="object[]">

Массив аргументов метода. Каждый аргумент передаётся объектом с индексом 0, 1 и т. д.; значение должно соответствовать Java-типу параметра.

</ResponseField>

## Endpoint и авторизация

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Доступ:** ADMIN. **Bearer Token:** требуется.

- GUEST — без логина; доступен только whitelist.
- USER — требуется авторизация; доступны методы с проверкой USER.
- ADMIN — полный доступ к public-методам всех сервисов.
- Если метод проверяет роль внутри логики, GUEST может получить null.

## Методы

<ResponseField name="generateSiteMap" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `siteMapRequest` | `LinkedHashMap<String, Object>` | Передаётся через `args[0].0`. |

### generateSiteMap

```json
{
  "beanId": "sitemapGeneratorServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "generateSiteMap",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getPageLinks" type="HashSet<String>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `baseURL` | `String` | Передаётся через `args[0].0`. |
| `URL` | `String` | Передаётся через `args[1].1`. |
| `links` | `HashSet<String>` | Передаётся через `args[2].2`. |
| `urlsToIgnore` | `List<String>` | Передаётся через `args[3].3`. |

### getPageLinks

```json
{
  "beanId": "sitemapGeneratorServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getPageLinks",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="isWorkInProgress" type="boolean">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| — | — | Метод не принимает аргументы. |

### isWorkInProgress

```json
{
  "beanId": "sitemapGeneratorServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "isWorkInProgress",
  "args": []
}
```

</ResponseField>

<ResponseField name="changeStatus" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `status` | `ServiceState` | Передаётся через `args[0].0`. |

### changeStatus

```json
{
  "beanId": "sitemapGeneratorServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "changeStatus",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
