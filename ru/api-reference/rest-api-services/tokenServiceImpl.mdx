---
title: 'TokenServiceImpl'
description: 'Вызов публичных методов сервиса через REST API: tokenServiceImpl'
---

# TokenServiceImpl

<ResponseField name="beanId" type="string">

Идентификатор Spring bean, который содержит вызываемый сервис.

tokenServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Область действия bean. Для вызовов сервисов используется PROTOTYPE.

</ResponseField>

<ResponseField name="functionName" type="string">

Имя публичного метода сервиса, указанное в сигнатуре ниже.

</ResponseField>

<ResponseField name="args" type="object[]">

Массив аргументов метода. Каждый аргумент передаётся объектом с индексом 0, 1 и т. д.; значение должно соответствовать Java-типу параметра.

</ResponseField>

## Endpoint и авторизация

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Доступ:** ADMIN. **Bearer Token:** требуется.

- GUEST — без логина; доступен только whitelist.
- USER — требуется авторизация; доступны методы с проверкой USER.
- ADMIN — полный доступ к public-методам всех сервисов.
- Если метод проверяет роль внутри логики, GUEST может получить null.

## Методы

<ResponseField name="generateToken" type="String">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `tokenType` | `TokenType` | Передаётся через `args[0].0`. |

### generateToken

```json
{
  "beanId": "tokenServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "generateToken",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="createVerificationTokenForUser" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `user` | `User` | Передаётся через `args[0].0`. |
| `token` | `String` | Передаётся через `args[1].1`. |

### createVerificationTokenForUser

```json
{
  "beanId": "tokenServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "createVerificationTokenForUser",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="createPasswordResetTokenForUser" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `user` | `User` | Передаётся через `args[0].0`. |
| `token` | `String` | Передаётся через `args[1].1`. |

### createPasswordResetTokenForUser

```json
{
  "beanId": "tokenServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "createPasswordResetTokenForUser",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getVerificationToken" type="Optional<VerificationToken>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `verificationToken` | `String` | Передаётся через `args[0].0`. |

### getVerificationToken

```json
{
  "beanId": "tokenServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getVerificationToken",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getVerificationToken" type="Optional<VerificationToken>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `verificationToken` | `String` | Передаётся через `args[0].0`. |
| `username` | `String` | Передаётся через `args[1].1`. |

### getVerificationToken

```json
{
  "beanId": "tokenServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getVerificationToken",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getPasswordResetToken" type="Optional<PasswordResetToken>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `passwordResetToken` | `String` | Передаётся через `args[0].0`. |

### getPasswordResetToken

```json
{
  "beanId": "tokenServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getPasswordResetToken",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="getPasswordResetToken" type="Optional<PasswordResetToken>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `passwordResetToken` | `String` | Передаётся через `args[0].0`. |
| `username` | `String` | Передаётся через `args[1].1`. |

### getPasswordResetToken

```json
{
  "beanId": "tokenServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "getPasswordResetToken",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="findByUser" type="Optional<PasswordResetToken>">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `user` | `User` | Передаётся через `args[0].0`. |

### findByUser

```json
{
  "beanId": "tokenServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "findByUser",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="deleteVerificationToken" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `verificationToken` | `VerificationToken` | Передаётся через `args[0].0`. |

### deleteVerificationToken

```json
{
  "beanId": "tokenServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "deleteVerificationToken",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="deletePasswordResetToken" type="void">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `passwordResetToken` | `PasswordResetToken` | Передаётся через `args[0].0`. |

### deletePasswordResetToken

```json
{
  "beanId": "tokenServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "deletePasswordResetToken",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
