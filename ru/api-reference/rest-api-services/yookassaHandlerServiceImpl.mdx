---
title: 'YookassaHandlerServiceImpl'
description: 'Вызов публичных методов сервиса через REST API: yookassaHandlerServiceImpl'
---

# YookassaHandlerServiceImpl

<ResponseField name="beanId" type="string">

Идентификатор Spring bean, который содержит вызываемый сервис.

yookassaHandlerServiceImpl

</ResponseField>

<ResponseField name="scope" type="string" default="PROTOTYPE">

Область действия bean. Для вызовов сервисов используется PROTOTYPE.

</ResponseField>

<ResponseField name="functionName" type="string">

Имя публичного метода сервиса, указанное в сигнатуре ниже.

</ResponseField>

<ResponseField name="args" type="object[]">

Массив аргументов метода. Каждый аргумент передаётся объектом с индексом 0, 1 и т. д.; значение должно соответствовать Java-типу параметра.

</ResponseField>

## Endpoint и авторизация

```http
POST /api/v1/bean/request
Content-Type: application/json
```

**Доступ:** ADMIN. **Bearer Token:** требуется.

- GUEST — без логина; доступен только whitelist.
- USER — требуется авторизация; доступны методы с проверкой USER.
- ADMIN — полный доступ к public-методам всех сервисов.
- Если метод проверяет роль внутри логики, GUEST может получить null.

## Методы

<ResponseField name="handlePayoutPaidEvent" type="Object">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `JSONObject` | Передаётся через `args[0].0`. |
| `httpSession` | `HttpSession` | Передаётся через `args[1].1`. |

### handlePayoutPaidEvent

```json
{
  "beanId": "yookassaHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handlePayoutPaidEvent",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handlePaymentFailedEvent" type="Object">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `JSONObject` | Передаётся через `args[0].0`. |

### handlePaymentFailedEvent

```json
{
  "beanId": "yookassaHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handlePaymentFailedEvent",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handlePaymentSucceededEvent" type="Object">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `JSONObject` | Передаётся через `args[0].0`. |
| `httpSession` | `HttpSession` | Передаётся через `args[1].1`. |

### handlePaymentSucceededEvent

```json
{
  "beanId": "yookassaHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handlePaymentSucceededEvent",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handlePaymentCreatedEvent" type="Object">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `JSONObject` | Передаётся через `args[0].0`. |

### handlePaymentCreatedEvent

```json
{
  "beanId": "yookassaHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handlePaymentCreatedEvent",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handleSubscriptionCanceledEvent" type="Object">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `JSONObject` | Передаётся через `args[0].0`. |

### handleSubscriptionCanceledEvent

```json
{
  "beanId": "yookassaHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleSubscriptionCanceledEvent",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handleSubscriptionDeletedEvent" type="Object">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `JSONObject` | Передаётся через `args[0].0`. |

### handleSubscriptionDeletedEvent

```json
{
  "beanId": "yookassaHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleSubscriptionDeletedEvent",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>

<ResponseField name="handleCustomerDeletedEvent" type="Object">

| Аргумент | Тип | Описание |
| --- | --- | --- |
| `request` | `JSONObject` | Передаётся через `args[0].0`. |

### handleCustomerDeletedEvent

```json
{
  "beanId": "yookassaHandlerServiceImpl",
  "scope": "PROTOTYPE",
  "functionName": "handleCustomerDeletedEvent",
  "args": [
    {
      "0": null
    }
  ]
}
```

</ResponseField>
