---
title: "msc-hero-container"
description: "Компонент редактора (msc-*)."
---

`msc-hero-container` — компонент, ориентированный на использование в редакторе (Lit).

Для него пока нет отдельного документационного аналога из `msm-*`. См. также [msc-components](./msc-components).

## Использование

```html
<msc-hero-container></msc-hero-container>
```

