---
title: "msc-sku-filter"
description: "Компонент редактора (msc-*)."
---

`msc-sku-filter` — компонент, ориентированный на использование в редакторе (Lit).

Для него пока нет отдельного документационного аналога из `msm-*`. См. также [msc-components](./msc-components).

## Использование

```html
<msc-sku-filter></msc-sku-filter>
```

