---
title: "msm-consent-banner"
description: "Web-компонент msm-consent-banner."
---

`msm-consent-banner` — web-компонент на Lit.

Согласия/настройки cookies.

```html
<msm-consent-banner></msm-consent-banner>
```

Атрибуты / свойства

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `cookies-page-url` | string | - |  |
| `theme` | string | - |  |
| `color-scheme` | string | - |  |

События

Этот компонент не генерирует собственных CustomEvent.

CSS parts

- `base`

Слоты

Документированных слотов нет.

Методы

Публичные методы не документированы.
