---
title: "msm-select-item"
description: "Web-компонент msm-select-item."
---

`msm-select-item` — web-компонент на Lit.

Поля форм и элементы управления.

```html
<msm-select-item value="msm-select-item"></msm-select-item>
```

Атрибуты / свойства

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `theme` | string | - |  |
| `type` | MenuItemType | - |  |
| `value` | string | - |  |
| `selected` | boolean | - |  |

События

| Event | detail | Когда |
| --- | --- | --- |
| `msm-item-change` | `label`, `selected` |  |

Пример

```html
<msm-select-item value="msm-select-item">
</msm-select-item>

<script>
  const el = document.querySelector("msm-select-item");
  el.addEventListener("msm-item-change", (e) => {
    console.log(e.detail);
  });

</script>
```

CSS parts

- `base`
- `label`
- `prefix`
- `suffix`

Слоты

- `slot[name="prefix"]`
- `slot`
- `slot[name="suffix"]`

Методы

- `validate()`

Примечания

- Custom events bubble.
