---
title: "msm-small-container"
description: "Web-компонент msm-small-container."
---

`msm-small-container` — web-компонент на Lit.

Переиспользуемый UI-блок.

```html
<msm-small-container></msm-small-container>
```

Атрибуты / свойства

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `padding-top` | number | `0` |  |
| `padding-bottom` | number | `0` |  |

События

Этот компонент не генерирует собственных CustomEvent.

CSS parts

Документированных `part`-слотов нет.

Слоты

- `slot[name="small-container"]`

Методы

Публичные методы не документированы.
