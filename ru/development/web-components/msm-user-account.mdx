---
title: "msm-user-account"
description: "Web-компонент msm-user-account."
---

`msm-user-account` — web-компонент на Lit.

UI-блоки для пользователя/аккаунта.

```html
<msm-user-account></msm-user-account>
```

Атрибуты / свойства

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `theme` | string | - |  |
| `color-scheme` | string | - |  |
| `variant` | string | - |  |
| `display` | string | - |  |
| `behavior` | string | - |  |

События

Этот компонент не генерирует собственных CustomEvent.

CSS parts

Документированных `part`-слотов нет.

Слоты

Документированных слотов нет.

Методы

Публичные методы не документированы.
