---
title: "msm-video-block"
description: "Web-компонент msm-video-block."
---

`msm-video-block` — web-компонент на Lit.

Отображение медиа (изображения/видео).

```html
<msm-video-block></msm-video-block>
```

Атрибуты / свойства

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `video-link` | string | - |  |
| `margin-top` | number | `0` |  |
| `margin-bottom` | number | `0` |  |

События

Этот компонент не генерирует собственных CustomEvent.

CSS parts

Документированных `part`-слотов нет.

Слоты

- `slot[name="image"]`

Методы

Публичные методы не документированы.
